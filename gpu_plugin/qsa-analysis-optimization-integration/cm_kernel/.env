# Optional template only; the harness does not load this file automatically.
# Replace with the directory containing libclangFEWrapper.so.
# The tested remote B580 uses /mnt/river (set explicitly in the test commands).
CM_FE_DIR=/path/to/cm-frontend